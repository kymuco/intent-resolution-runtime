name: Materialize M1.4 R8 hardening fix

on:
  push:
    branches:
      - m1.4/binding-symbolic-reference-ir

permissions:
  contents: write

jobs:
  materialize:
    if: ${{ contains(github.event.head_commit.message, 'm1.4/r8-materializer-fix-stage') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Build hardened source tree and commit object
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: python tools/r8_materialize.py
